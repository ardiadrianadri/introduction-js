/**
 * Code a class to manage a linked list. A linked list is a list which last element + 1 is the first element and the 
 * first element - 1 is the last element.
 * The class must have:
 * An attribute which retruns the lenght of the list
 * A search method which return the value stored in a position of the list
 * A add method which add an element in the queu of the list
 * A remove method which remove an element from the begin of the list
 * 
 * A List can be initiated with an Array but it should be possible to create a empty list with nothing, too
 */